{
  "name": "css-renderer-test",
  "packageManager": "yarn@3.6.3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "ngrok": "yarn dev && ngrok http --bind-tls true --region eu --host-header rewrite https://localhost:3000"
  },
  "devDependencies": {
    "@types/three": "^0.155.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "^1.0.1",
    "three": "^0.155.0"
  }
}
